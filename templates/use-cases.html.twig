
Model Documentation for: {{ namespace }}
==

{% if data.Patterns_Command is defined %}
## Use-Cases (application services)

{% for result in data.Patterns_Command %}
#### {{ result.shortClass }}
_{{ result.attribute.value|default('No description available') }}_

`{{ namespace }}{{ result.class }}`

{% endfor %}
{% endif %}
